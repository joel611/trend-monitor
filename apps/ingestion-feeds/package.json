{
	"name": "@trend-monitor/ingestion-feeds",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev --persist-to ../../.wrangler-shared/state -c wrangler.toml -c ../processor-worker/wrangler.toml",
		"build": "tsc && wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"test": "bun test",
		"test:unit": "bun test",
		"test:integration": "bun test",
		"test:watch": "bun test --watch",
		"test:coverage": "bun test --coverage",
		"typecheck": "tsc --noEmit",
		"lint": "biome check src",
		"format": "biome check src",
		"format:fix": "biome check --write src",
		"wrangler:types": "wrangler types"
	},
	"dependencies": {
		"@trend-monitor/config": "workspace:*",
		"@trend-monitor/db": "workspace:*",
		"@trend-monitor/types": "workspace:*",
		"@trend-monitor/utils": "workspace:*",
		"rss-parser": "^3.13.0"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "latest",
		"@types/bun": "latest",
		"typescript": "^5.7.0",
		"wrangler": "latest"
	}
}
